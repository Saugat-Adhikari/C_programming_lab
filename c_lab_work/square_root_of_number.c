#include<stdio.h>
#include<math.h>
int main(){
   //  find the square root of the inputed number .
   int num = 16;
   printf("--> the squareroot of the given number is :  %.2lf", sqrt(num));
    return 0;
}